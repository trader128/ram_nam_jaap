# Copy to functions/.env.local (not functions/.env).
# Firebase deploys .env as Cloud Run env vars; the same names are also
# secrets, so a production .env collides. .env.local stays on this machine.
# Dashboard: https://api.prokerala.com/account/client
PROKERALA_CLIENT_ID=
PROKERALA_CLIENT_SECRET=
